node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Python venv for scripts/
.venv/

# Voice clips from scripts/generate_tts.py. The spoken "A is for apple" prompts
# under voice/phrase/ ARE committed -- the app plays them and the PWA precaches
# them, so they have to ship. Everything else the script can make is scratch.
static/sounds/voice/*
!static/sounds/voice/phrase/
